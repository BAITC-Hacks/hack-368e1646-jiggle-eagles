{
  "name": "@hackalem/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../.env --watch --import tsx src/server.ts --dev",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json",
    "build": "tsc",
    "start": "node --env-file-if-exists=../.env dist/server.js"
  },
  "dependencies": {
    "express": "5.2.1",
    "openai": "7.21.0",
    "zod": "4.6.5"
  },
  "devDependencies": { "@types/express": "5.0.6" }
}
