# Money Graph implementation ownership

## Separate analysis pages (MG-UI-04)

The current saved-analysis task is the sole integrator, implementer and acceptance/testing owner for the user's follow-up requiring separate list and detail pages. Following completion of MG-UI-03, it owns routing edits in `money_graph/server.py`, `money_graph/static/{app.js,index.html,file-preview.js}`, `tests/{test_money_graph.py,browser_money_graph.py,browser_themes.py}`, this file, `docs/hackathon/requirements.md`, `disclosures/README.md` and a new page-routing disclosure. It preserves the completed design, metadata, localization, other tasks' changes and the index. README remains with its documentation task. No parallel agents or dependencies are introduced.

Contract: `/analyses` shows uploads and history; `/analyses/<startup|32-hex-id>` shows only the selected analysis. `/` opens the list with a canonical `/analyses` URL. Direct links, refresh and browser Back/Forward obey the URL; a successful upload opens its own result URL. A missing/damaged run shows an error with navigation back to the list, preserving existing results. Existing open/status/history APIs and saved artifact formats remain unchanged; result-page export links include an optional `revision` query so a different active run returns 409 instead of the wrong file. Acceptance covers deep links after restart, hidden unrelated page content, upload navigation, cached graph preservation, failed opens, revision-checked exports, themes/locales and all existing journeys.

## Freedom-inspired interface and themes (MG-UI-03)

The user-authorized “Plan Freedom-inspired design” task is the sole integrator, implementer and acceptance/testing owner for the approved redesign. The saved-analysis task has completed its source edits; this task takes over `money_graph/static/{index.html,style.css,app.js,file-preview.js,theme.js}`, `money_graph/static/locales/{en,kk,ru}.json`, `money_graph/server.py` (static theme asset registration only), `tests/browser_money_graph.py`, `tests/browser_file_preview.py`, `tests/browser_themes.py`, `README.md`, this file, `docs/hackathon/requirements.md`, `disclosures/README.md` and a new timestamped design disclosure. All preceding upload/history/details, localization and disclosure-reorganization changes and the index are preserved. No parallel agents or dependency changes are needed.

Presentation contract: compact header, rounded upload/history surfaces and a focused results view with navigation back to analyses; all existing uploads, saved metadata, investigation controls and exports remain available. Light uses the sampled Freedom Bank core palette; dark is the approved Money Graph adaptation. Light/Dark/System choice defaults to System, persists locally when storage is available, follows OS changes in System mode, and changes only presentation. SVG categories, labels, arrows, boundary markers and selection stay legible without resetting graph state. Acceptance covers both themes, preference/fallback behavior, view navigation, EN/KZ/RU, keyboard focus, narrow layouts and the existing real browser journey. No API/data calculation semantics change. README ownership is transferred to the user-authorized “Create complete project README” task for its judge-facing rewrite; the design task does not edit README.

## General interface and saved analyses (MG-UI-01)

For this change, the current Codex task is the sole integrator, implementer and acceptance/testing owner. It owns edits to `money_graph/server.py`, `money_graph/static/{index.html,style.css,app.js,i18n.js}`, `money_graph/static/locales/{en,kk,ru}.json`, `tests/test_money_graph.py`, `tests/browser_money_graph.py`, `README.md`, `disclosures/**`, this file and `docs/hackathon/requirements.md`. Existing launch-related edits and the index are preserved. No parallel agents or dependency changes are needed.

Contract: the upload panel precedes a newest-first list of successful saved analyses. Every successful upload retains its unique UUID; a SHA-256 fingerprint over the three named input-file hashes marks repeated byte-identical datasets without reusing or overwriting a run. `GET /api/analyses` returns `analyses` with ID, UTC start timestamp, counts, duration in seconds, fingerprint, earliest matching run ID and active flag, plus an unreadable-entry count. `POST /api/analyses/<id>/open` (empty body, same-origin loopback only) verifies stored artifact hashes and atomically switches the active results/downloads; missing IDs return 404, busy processing 409, damaged saved results 422. History uses existing local run directories, survives restart and includes available startup outputs; an upload-only restart starts with no selected analysis. Failed uploads stay in the upload error/status area and do not create history entries. Acceptance covers ordering, distinct IDs for duplicates, restart/reopen with original export bytes, failed reopen preservation, localized empty/history/error states and mobile layout.

MG-UI-02 extends the same ownership with editable saved-analysis titles and optional descriptions. `POST /api/analyses/<id>/details` accepts a same-origin JSON object with a required, trimmed, single-line title (1–120 characters) and an optional description (0–2,000 characters; omitted or blank clears it), with a 16 KiB request limit. It returns the saved history entry; invalid input returns 400, missing runs 404, oversized requests 413, unsupported content type 415 and storage failures 500. Details are atomically stored in `analysis_details.json`, separate from reproducible artifacts, and associated with the manifest start timestamp so a new CLI startup run does not inherit old details. Existing runs retain their localized date-based title until edited. Acceptance covers save/cancel, clearing descriptions, validation, plain-text rendering, language changes, restart persistence and unchanged IDs, hashes and exports.

The “Launch the app” task is the sole integration/configuration owner and acceptance owner for ENG-02–03. The “Implement Money Graph v2” task is the feature implementer and acceptance/testing owner for MG-01–09, MG-V2-01–04 and ENG-01. Shared checkout Git staging, commits and pushes are prohibited by the current user request; neither task performs them. Root checks are run by the setup task to avoid concurrent output writes.

The same ownership covers minimal v2 (MG-V2-01–04): uploads and publication, bounded graph expansion, measured cluster descriptions and browser acceptance. No dependency changes are needed.

Setup task exclusive ownership: `.python-version`, `scripts/setup.sh`, `scripts/check_environment.py`, `scripts/money-graph.sh`, `tests/test_setup.py`, `.github/workflows/jiggles-ci.yml`, the introductory comment in `requirements-money-graph.txt`, startup guidance in `AGENTS.md`, `docs/hackathon/deployment-readiness.md` and `docs/hackathon/integration-verification.md`. Those paths override the broader original assignment below. V2 owns `money_graph/**`, `tests/test_money_graph.py`, `tests/browser_money_graph.py`, and the methodology/architecture documentation. After the feature handoff, setup owns README, disclosures, requirements and this assignment file for setup-specific edits, preserving all v2 feature content. No dependency installation or build output writes run concurrently.

Owned implementation paths: `money_graph/__init__.py`, `money_graph/__main__.py`, `money_graph/pipeline.py`, `money_graph/server.py`, `money_graph/static/index.html`, `money_graph/static/style.css`, `money_graph/static/app.js`, `tests/test_money_graph.py`, `tests/browser_money_graph.py`, `requirements-money-graph.txt`, `requirements-money-graph-test.txt`, `scripts/money-graph.sh`, `.gitignore`, `README.md`, `disclosures/**`.

Owned documentation paths: this file, `docs/hackathon/requirements.md`, `docs/hackathon/data-profile.md`, `docs/money-graph/methodology.md`, `docs/money-graph/architecture.md`, `docs/money-graph/README.md`. Inputs, source archives and generated results are owned under ignored `data/private/money-graph/`. Existing Node/React code, index and unrelated private files are preserved. The user-provided `docs/money-graph/task.md` is read-only.

## Localization ownership and contract (MG-I18N-01)

The current localization task (Codex) is the sole integrator, implementer and acceptance/testing owner for this change. It takes ownership of localization edits in `money_graph/pipeline.py`, `money_graph/server.py`, `money_graph/i18n.py`, `money_graph/static/index.html`, `money_graph/static/style.css`, `money_graph/static/app.js`, `money_graph/static/i18n.js`, `money_graph/static/locales/en.json`, `money_graph/static/locales/kk.json`, `money_graph/static/locales/ru.json`, `tests/test_localization.py`, `tests/browser_money_graph.py`, `README.md`, `disclosures/**`, this file and `docs/hackathon/requirements.md`. Existing setup/v2 work and the Git index are preserved. The related direct-file preview task owns only `money_graph/static/file-preview.js` and `tests/browser_file_preview.py`; localization integrates its classic loader in the owned index/server paths. The setup task retains exclusive Git staging/commit ownership for its separately authorized setup-only commit.

The dashboard uses local JSON catalogs for English (`en`), Kazakh (`kk`, displayed as KZ) and Russian (`ru`). English is the fallback; a saved selection takes precedence over the browser language. Language changes preserve current inspection, graph state and chosen upload files. Static labels, accessibility text, dynamic explanations and validation/status messages use message keys and named placeholders. APIs retain their existing English text and add message descriptors for client rendering; cluster descriptions expose structured message parts. IDs, numeric values, role identifiers and the official CSV schemas/content remain language-independent. No dependency changes or external translation service are needed. Acceptance covers catalog key/placeholder parity, locale fallback/persistence, real browser switching and translated success/error/empty/account/graph states, with existing Python and Node checks.

## Agreed local contract

- Python CLI reads the three documented Parquet files, validates before calculating, writes the three official CSV schemas and a deterministic dashboard JSON. A separate run manifest records hashes, versions, hardware and timing.
- Python standard-library HTTP server binds only to `127.0.0.1`. `GET /api/overview` returns counts, cluster summaries and ranked accounts; `GET /api/account?gid=<exact decimal int64>` returns measured features, role rules, priority contributions, all incident directed edges and a bounded one-hop graph. `GET /api/graph?gid=<id>&hops=1|2&revision=<active revision>` returns up to 50 accounts (center included), all directed edges between them, hop distances, eligible/omitted counts and the limit. Neighbors in either direction define distance; breadth-first selection breaks ties by exact numeric gid. IDs remain decimal strings. Invalid queries return 400, missing IDs/routes 404, stale revisions 409 and no active analysis 503.
- `POST /api/analysis` accepts exactly three multipart fields named `nodes`, `edges`, `transactions`, with corresponding `.parquet` filenames, at most 64 MiB total including framing. Host and Origin must match the loopback server. It returns 202 after receipt; one job runs at a time (409 otherwise). `GET /api/status` reports receiving/validating/analyzing/exporting/succeeded/failed, an error/message and active revision. All parsing, validation, analysis, exports and manifest creation finish in a separate local directory before a single locked active-snapshot swap. Failed jobs preserve active API data and download bytes. Successful upload inputs/outputs remain under `<out>/uploads/<run-id>/{input,output}` for reproduction; startup outputs are preserved. No database or external service is involved.
- CSV uses UTF-8, LF, exact decimal int64 IDs, fixed six-decimal scores and JSON arrays of decimal strings for `top_gids`. Amounts are observed KZT; dates have day precision with no timezone. Graph direction is payer → recipient; all supplied nodes are added first.
- pandas and PyArrow load/validate Parquet; NetworkX builds the directed graph and seeded weighted Louvain communities. Exact dependency pins are in the two requirements files; Playwright is test-only. No new Node dependencies, external assets or runtime network services.
- Acceptance: strict schemas and reconciliation; synthetic motifs and boundary counterexamples; all-node coverage including isolates; at least 20 ranked official accounts; byte-identical repeated CSV/JSON output; timed official run below 300 seconds; real HTTP and automated browser journey; existing root typecheck/tests/build. Remote CI remains a gap until a user-authorized push.

- Local setup contract (ENG-03): `.python-version` pins the existing verified interpreter; setup creates or checks `.venv`, installs the unchanged exact requirements and verifies dependencies. Launch validates runtime/package versions without installing anything. CI uses the same setup and separate synthetic Python/browser checks. No application API or data semantics change.


## Temporal analysis ownership (MG-TIME-01)

For this user-requested change, the current Codex task is the sole integrator, implementer and acceptance owner. Ownership transfers for `money_graph/temporal.py`, `money_graph/pipeline.py`, `money_graph/static/app.js`, `money_graph/static/locales/en.json`, `money_graph/static/locales/ru.json`, `money_graph/static/locales/kk.json`, `tests/test_temporal.py`, `tests/browser_temporal.py`, the role fixture in `tests/test_money_graph.py`, `README.md`, `disclosures/**`, `docs/money-graph/methodology.md`, `docs/hackathon/requirements.md` and this file. No parallel agents or runtime dependency changes are involved.

Additive account/dashboard JSON contract: `temporal` contains KZT amounts, nullable matched-incoming share, dated FIFO matches, independent same-day overlaps, and late-window incoming KZT as specified in methodology. Existing routes, errors, exact string IDs and CSV columns remain unchanged. Backend calculation and localized frontend presentation are integrated in this task; acceptance uses hand-computed temporal examples and the real browser/backend journey.

The same sole-owner scope covers MG-PATTERN-01 and the selector update in `tests/browser_money_graph.py`. Account/dashboard JSON additionally exposes `patterns` message descriptors with raw numeric evidence; no endpoint or CSV-schema changes.
